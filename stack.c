#include "monty.h"
/**
 * f_stack - does prints the top
 * @head: stack head
 * @count: line_num
 * Return: nothing
*/
void f_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 0;
}
